<?xml version="1.0" encoding="utf-8"?><!--
 no avatar
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	android:id="@+id/msg_list_item_status"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:orientation="vertical"
	android:paddingLeft="16dp"
	android:paddingRight="16dp"
	android:paddingBottom="5dp">

	<LinearLayout
		android:id="@+id/message_block"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:layout_gravity="center"
		android:background="@drawable/bubble_group_status_selector"
		android:orientation="horizontal"
		android:paddingLeft="12dp"
		android:paddingTop="@dimen/message_status_padding_top"
		android:paddingRight="12dp"
		android:paddingBottom="@dimen/message_status_padding_bottom">

		<ImageView
			android:layout_width="18dp"
			android:layout_height="18dp"
			android:layout_marginRight="4dp"
			android:baselineAlignBottom="true"
			android:layout_gravity="center_vertical"
			android:importantForAccessibility="no"
			android:src="@drawable/ic_group_call"
			app:tint="?attr/textColorSecondary" />

		<ch.threema.app.emojis.EmojiConversationTextView
			android:id="@+id/text_view"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:autoLink="none"
			android:ellipsize="end"
			android:gravity="center_horizontal"
			android:maxLines="2"
			android:text="Text"
			android:textColor="?attr/textColorPrimary"
			android:textSize="?attr/font_status" />

	</LinearLayout>

</LinearLayout>
